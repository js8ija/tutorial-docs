---
title: "8. 手动 MCP 配置示例"
description: "CodeGraph：8. 手动 MCP 配置示例"
---
<!-- This file is generated from the workspace tutorial source. Edit the source Markdown or generator. -->
## 8. 手动 MCP 配置示例

一般推荐用安装器自动配置：

```bash
npx @colbymchenry/codegraph
```

如果想手动配置，可以先全局安装：

```bash
npm install -g @colbymchenry/codegraph
```

然后把 MCP Server 配成：

```json
{
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "codegraph",
      "args": ["serve", "--mcp"]
    }
  }
}
```

Claude Code 还可以把只读工具加入 allow 列表：

```json
{
  "permissions": {
    "allow": [
      "mcp__codegraph__codegraph_search",
      "mcp__codegraph__codegraph_context",
      "mcp__codegraph__codegraph_callers",
      "mcp__codegraph__codegraph_callees",
      "mcp__codegraph__codegraph_impact",
      "mcp__codegraph__codegraph_node",
      "mcp__codegraph__codegraph_status",
      "mcp__codegraph__codegraph_files"
    ]
  }
}
```

注意：官方 Integrations 文档提示 Cursor 手动配置时需要特别注意工作目录问题；安装器会自动处理。如果手动接 Cursor，通常要显式传项目路径。
